id: ceae095a-906d-44f6-a277-469499783ec4
name: deleteRule
request:
  url:
    raw: >-
      {{API_Base_URL}}/system/{{SYSTEM_ID}}/device/{{EquipmentManagement.hospitalDetails.hospitalId}}/srv/prosightBypass/rules/delete
    host:
      - '{{API_Base_URL}}'
    path:
      - system
      - '{{SYSTEM_ID}}'
      - device
      - '{{EquipmentManagement.hospitalDetails.hospitalId}}'
      - srv
      - prosightBypass
      - rules
      - delete
  method: PUT
  headers:
    - key: sec-ch-ua-platform
      value: '"Windows"'
    - key: Accept
      value: application/json
    - key: Content-Type
      value: application/json
  body:
    mode: raw
    modeOfBody: JSON
    raw: >-
      {"equipmentId":"{{EquipmentManagement.equipmentDetails.equipment3.equipmentId}}","isInspection":true}
  auth:
    type: bearer
    bearer:
      token: '{{idToken}}'
events: []
type: request
